<p style="color: green"><%= notice %></p>

<h1>試合分析詳細</h1>

<%= render partial: "match_info_detail", locals: { match_info: @match_info } %>

<div>
  <%= link_to "編集", edit_match_info_path(@match_info) %> |
  <%= link_to "分析一覧ページへ", match_infos_path %>

  <%= button_to "削除", @match_info, method: :delete %>
</div>
